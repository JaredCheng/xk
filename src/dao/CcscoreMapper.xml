<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dao.CcscoreMapper" >
  <resultMap id="BaseResultMap" type="model.Ccscore" >

    <result column="STUID" property="stuid" jdbcType="NUMERIC" />
    <result column="COUID" property="couid" jdbcType="NUMERIC" />
    <result column="SCORE" property="score" jdbcType="NUMERIC" />
  </resultMap>
  <insert id="insert" parameterType="model.Ccscore" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into CCSCORE (STUID, COUID, SCORE
      )
    values (#{stuid,jdbcType=NUMERIC}, #{couid,jdbcType=NUMERIC}, #{score,jdbcType=NUMERIC}
      )
  </insert>
  <insert id="insertSelective" parameterType="model.Ccscore" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into CCSCORE
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="stuid != null" >
        STUID,
      </if>
      <if test="couid != null" >
        COUID,
      </if>
      <if test="score != null" >
        SCORE,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="stuid != null" >
        #{stuid,jdbcType=NUMERIC},
      </if>
      <if test="couid != null" >
        #{couid,jdbcType=NUMERIC},;
      </if>
      <if test="score != null" >
        #{score,jdbcType=NUMERIC},
      </if>
    </trim>
  </insert>
    <insert id="add" parameterType="java.util.List">
     insert into CCSCORE
    <foreach collection="list" separator="UNION" item="t">
      <if test="t.couid !=null">
      SELECT #{t.stuid,jdbcType=NUMERIC} STUID,#{t.couid,jdbcType=NUMERIC} COUID,#{t.score,jdbcType=NUMERIC} SCORE from dual
      </if>
    </foreach>
    </insert>
  <update id="update" parameterType="java.util.List">
    begin
    <foreach collection="list" item="item" index="index" separator=";" >
      update CCSCORE
      <set>
        <if test="item.score!=null">
          SCORE = #{item.score},
        </if>
      </set>
      where couid = #{item.couid} and stuid = #{item.stuid}
    </foreach>
    ;end;
  </update>
  <select id="queryStudent" resultType="model.Ccscore" parameterType="java.math.BigDecimal">
     SELECT *
     FROM CCSCORE
     WHERE COUID =#{couid,jdbcType=NUMERIC}
    </select>
</mapper>